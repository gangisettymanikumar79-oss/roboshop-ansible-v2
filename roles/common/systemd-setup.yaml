- name: "copy {{ compoent }} server"
  template:
    src: "{{ compoent }}.server.j2"
    dest: "/etc/systemd/system/{{ compoent }}.service"

- name: deamon reload
  systemd_service:
      name: "{{ compoent }}"
      daemon_reload: true
    