- name: "copy {{ component }} server"
  template:
    src: "{{ component }}.server.j2"
    dest: "/etc/systemd/system/{{ component }}.service"

- name: deamon reload
  systemd_service:
      name: "{{ component }}"
      daemon_reload: true
      
      